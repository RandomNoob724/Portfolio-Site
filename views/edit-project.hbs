<main>
    <div class="jumbotron container" id="create-post-form">
        <form class="form-createpost form" method="POST" action="/portfolio/{{project.id}}/edit">
            <h1 class="text-center">Edit project</h1>
            <div class="text-center mb-4">
                {{#if validationErrors.length}}
                    <p>Errors:</p>
                    <ul>
                        {{#each validationErrors}}
                            <li>{{this}}</li>
                        {{/each}}
                    </ul>
                {{/if}}
            </div>
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <div class="form-label-group">
                <input name="projectName" type="text" class="form-control" placeholder="Blogpost Header" value="{{project.name}}" id="create-post-input-field">
            </div>
            <div class="form-label-group">
                <textarea name="projectDescription" class="form-control" id="create-post-input-field" rows="10" placeholder="Description">{{project.description}}</textarea>
            </div>
            <div class="form-label-group">
                <input name="projectLink" type="text" class="form-control" placeholder="Link to project" value="{{project.link}}" id="create-post-input-field">
            </div>
            <button class="btn btn-lg btn-primary btn-block" id="create-post-submitbutton" type="submit">Post</button>
        </form>
    </div>
</main>
